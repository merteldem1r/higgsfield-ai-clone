# secrets
.env
.env*.local
# from create-next-app: ignore every other .env variant too, but keep the template tracked
.env*
!.env.example
*.pem

# dependencies and build output
node_modules/
.next/
out/
.vercel/
/build
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# typescript
*.tsbuildinfo
next-env.d.ts

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Claude Code personal/local settings (hooks config in settings.json stays tracked)
.claude/settings.local.json

# OS noise
.DS_Store
